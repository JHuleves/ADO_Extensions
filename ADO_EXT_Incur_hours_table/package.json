{
  "name": "ado-imputa-horas-task",
  "version": "1.0.8",
  "description": "Azure DevOps Extension for Time Tracking",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf dist && mkdir dist",
    "copy-assets": "cp src/*.html dist/ && cp img/*.png dist/ && cp node_modules/vss-web-extension-sdk/lib/VSS.SDK.min.js dist/",
    "build": "npm run clean && tsc && npm run copy-assets",
    "package": "tfx extension create --manifest-globs vss-extension.json"
  },
  "devDependencies": {
    "@types/jquery": "^3.5.0",
    "tfx-cli": "^0.17.0",
    "typescript": "^4.0.0",
    "vss-web-extension-sdk": "^5.141.0"
  },
  "dependencies": {
    "azure-devops-extension-api": "^4.2.0",
    "azure-devops-extension-sdk": "^4.2.0"
  }
}
